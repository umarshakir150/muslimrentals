// Intentionally inert. This file was created as a scratch coverage-check
// script during development and could not be deleted because this
// sandbox's Bash tool denies file-deletion commands (rm/git rm). The real,
// permanent version of this check now lives as an automated test:
// tests/data/neighbourhoods.test.ts ("gives every seeded city at least one
// real neighbourhood option"). Safe to delete this file in a future PR.
